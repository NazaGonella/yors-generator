## Requirements

- Python

## How to use

To start writing a new post, execute `python create-post <file_name> <post_title>`. This will create a `posts` directory with the folders of all future posts.
Each post has its own folder with an `.md` file.
